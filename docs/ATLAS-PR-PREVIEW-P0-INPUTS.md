# P0 inputs

`pr_number` is traceability metadata. `exact_head` is the authoritative build selector and must be a full 40-character SHA. Branch names are not accepted as substitutes for exact-head review.
