# P0 compatibility

The static build reuses Atlas's existing `ATLAS_GITHUB_PAGES=true` export configuration only as a build mode. Canonical Pages deployment workflow and environment are not changed by P0.
