# P0 rationale

Separating provenance/build from hosting prevents a convenience feature from acquiring production authority. It also makes future provider replacement possible without changing the exact-head evidence contract.
