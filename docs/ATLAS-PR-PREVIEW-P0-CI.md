# P0 CI expectation

The infrastructure PR must trigger the new `Atlas PR Preview Policy` workflow because its own workflow/script/docs paths are changed. Existing repository gates remain authoritative as configured.
