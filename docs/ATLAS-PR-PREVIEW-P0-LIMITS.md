# P0 limits

P0 cannot be used for TalkBack/visual human review because an Actions artifact is not a hosted URL. That limitation is deliberate and is resolved only by a separately governed P1 publish adapter.
