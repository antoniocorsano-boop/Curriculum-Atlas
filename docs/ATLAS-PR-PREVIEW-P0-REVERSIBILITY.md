# P0 reversibility

P0 has no external hosting state. Removing the workflows/scripts/docs removes the capability; existing artifacts expire automatically. This is a deliberate prerequisite before introducing P1 provider state.
