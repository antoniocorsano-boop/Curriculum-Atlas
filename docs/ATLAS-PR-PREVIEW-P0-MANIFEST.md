# P0 manifest semantics

`PREVIEW-MANIFEST.json` is evidence metadata, not a publication manifest. Its `runtimeAuthorized`, `studentUseAuthorized` and `canonicalPagesDeployment` fields must remain `false` in P0.
