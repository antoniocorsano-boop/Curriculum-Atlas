# Atlas PR Preview P0 — traceability

Requirement → evidence:

- exact-head binding → workflow input validation + checkout equality + `EXACT_HEAD.txt`;
- provenance → `PREVIEW-MANIFEST.json`;
- production isolation → read-only permissions + policy script;
- reversibility → artifact-only P0, seven-day retention;
- non-authority → manifest flags + governance documents;
- G1 continuity → explicit PR #35 SHA handoff.
