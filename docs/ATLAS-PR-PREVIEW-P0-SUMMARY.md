# Atlas PR Preview P0 — summary

P0 adds a reusable exact-head static-build evidence layer for future human previews. It does not deploy a site.

Security posture: read-only, no persisted checkout credentials, no production environment, no Pages/provider write permission.

Governance posture: exact-head provenance, seven-day artifact, explicit non-authority.

Next only after PASS: P1 isolated URL adapter, first validated against frozen G1 PR #35 SHA.
